.next/
out/
build/
node_modules/
pnpm-lock.yaml
next-env.d.ts
public/

# Markdown is hand-wrapped and hand-aligned (the spec's tables are read as source as
# often as rendered). Prettier's table padding and prose rewrapping fight that, so
# formatting for docs is a human job here.
*.md

# Generated by scripts/build_brand.py. Formatting it would put Prettier and the
# generator in a loop: Prettier rewraps the path data, the next run of the script writes
# it back flat, and the file shows up dirty every time a mark is rebuilt. The generator
# emits it in the house style already.
lib/brand-marks.ts
